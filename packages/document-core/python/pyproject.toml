[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "formwise-document-core"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["pydantic>=2.0,<3"]

[tool.setuptools.packages.find]
where = ["src"]
